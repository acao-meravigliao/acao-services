{{page-title "Riassunto"}}
<div class="container">
  <div class="level">
    <div class="level-left">
      <div class="level-item">
        <table class="renew-services table">
          <thead>
            <tr>
              <th>Elemento</th>
              <th>Costo</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>{{this.ass_service.name}}</td>
              <td class="price">{{format-number this.ass_service.price style="currency" currency="EUR"}}</td>
            </tr>
            {{#if this.wizard.enable_cav}}
            <tr>
              <td>{{this.cav_service.name}}</td>
              <td class="price">{{format-number this.cav_service.price style="currency" currency="EUR"}}</td>
            </tr>
            {{/if}}

            {{#each this.wizard.services key="@index" as |service index|}}
            <tr>
              <td>
                {{service.type.name}}
                {{#if service.extra_info}}
                  <br />
                  {{service.type.extra_info}}: {{service.extra_info}}
                {{/if}}
              </td>
              <td class="price">{{format-number service.type.price style="currency" currency="EUR"}}</td>
            </tr>
            {{/each}}
          </tbody>
          <tfoot>
            <tr>
              <th>Totale</th>
              <td class="total price">{{format-number this.total style="currency" currency="EUR"}}</td>
            </tr>
          </tfoot>
        </table>
      </div>

      <div class="level-item">
        <div>
          <h2 class="subtitle">Turni di linea:</h2><br />
          {{#each this.wizard.selected_roster_days as |roster_day|}}
            {{format-date roster_day.date "ddd D/M"}}<br />
          {{/each}}
        </div>
      </div>
    </div>
  </div>

  <div class="buttons is-centered">
    <button class="button is-grey is-large" {{on "click" this.back}}>
      <span class="icon"><FaIcon @icon="backward" /></span>
      <span>Indietro</span>
    </button>
    <button class="button is-primary is-large" disabled={{this.submit_disabled}} {{on "click" this.submit}}>
      Conferma iscrizione!
    </button>
  </div>

  {{#if this.submit_error}}
  <div class="notification is-danger">
    <ExceptionDecode @ex={{this.submit_error}} as |title detail backtrace|>
      <div class="box notification is-danger">
        <h1 class="title">{{title}}</h1>
        <div>
          {{detail}}
        </div>
      </div>
    </ExceptionDecode>
  </div>
  {{/if}}
</div>
