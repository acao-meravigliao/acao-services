import Ember from 'ember';

export default Ember.Controller.extend({
//  allRosterDays: Ember.computed(function() {
//    return this.get('store').peekAll('ygg--acao--roster-day');
//  }),
//
//  todayRosterEntries: Ember.computed('filteredRosterEntries.@each', function() {
//    return this.get('filteredRosterEntries').filter((item) => (true)
////      (item.belongsTo('roster_day').value().get('date'))
//    );
//  }),

  actions: {
  },
});
